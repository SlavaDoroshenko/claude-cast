<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base TV theme -->
    <style name="Theme.ScreenMirrorTV" parent="@style/Theme.Leanback">
        <item name="colorPrimary">#7C6FFF</item>
        <item name="colorPrimaryDark">#0F0F1A</item>
        <item name="colorAccent">#00D4AA</item>
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_muted</item>
    </style>

    <!-- Fullscreen theme for MirrorActivity -->
    <style name="Theme.ScreenMirrorTV.Fullscreen" parent="@style/Theme.Leanback">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Card style for discovered devices -->
    <style name="Widget.ScreenMirrorTV.CardView" parent="Widget.Leanback.ImageCardView">
        <item name="cardBackgroundColor">@color/surface</item>
    </style>

</resources>
